"""vonto — an ontological-perspective companion to vconf.

Fresh implementation, not a copy of vconf: mirrors its conventions (module
boundaries, dataclass-heavy style, PROJECT_ROOT-relative paths) where those
conventions genuinely fit, but is being built up from scratch rather than
inheriting vconf's own accumulated history.
"""
